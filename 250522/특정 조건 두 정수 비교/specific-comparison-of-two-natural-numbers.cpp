#include <iostream>
using namespace std;

int main() {
    int a,b,c,d;
    cin >> a >> b;
    c = (a < b) ? 1 : 0;
    d = (a == b) ? 1 : 0;
    cout << c << " " << d;

    // Please write your code here.
    return 0;
}